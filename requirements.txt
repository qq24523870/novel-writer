# AI小说创作助手 - 依赖清单
# 安装命令: pip install -r requirements.txt

# 核心依赖
PySide6>=6.6.0

# AI模型相关
# OpenAI API (可选，使用OpenAI模型时需要)
openai>=1.0.0

# 本地大模型 (可选，使用本地GGUF模型时需要)
# llama-cpp-python>=0.2.0

# 阿里通义千问 (可选，使用通义千问时需要)
# dashscope>=1.0.0

# 文档导出
python-docx>=1.0.0

# HTTP请求 (文心一言API需要)
requests>=2.28.0